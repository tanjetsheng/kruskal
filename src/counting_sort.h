#ifndef _COUNTING_SORT_H
#define _COUNTING_SORT_H
#include <stdio.h>
#include <stdlib.h>


void printArray(int *array, int size);
int maximum(int * array, int size);
void countingSort(int * array, int size);

#endif // _COUNTING_SORT_H
